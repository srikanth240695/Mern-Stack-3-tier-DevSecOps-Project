version: 0.2

env:
  variables:
    TF_VERSION: "1.7.5"
    ENV: "dev"
    TF_DIR: "awscicd/jenkins-server"

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - echo "Installing Terraform ${TF_VERSION}"
      - wget -q https://releases.hashicorp.com/terraform/${TF_VERSION}/terraform_${TF_VERSION}_linux_amd64.zip
      - unzip -q terraform_${TF_VERSION}_linux_amd64.zip -d /usr/local/bin/
      - terraform --version

  pre_build:
    commands:
      - echo "ENV=$ENV | TF_DIR=$TF_DIR"
      - echo "Current dir $(pwd)"
      - ls -la backend/
      - terraform init -backend-config="backend/${ENV}.hcl" -reconfigure -input=false

  build:
    commands:
      - terraform validate
      - echo "Terraform Plan ENV=${ENV}"
      - terraform plan -var-file="env/jenkins-${ENV}.tfvars" -out=tfplan-${ENV} -input=false

  post_build:
    commands:
      - echo "Plan complete for ENV=${ENV}"

artifacts:
  base-directory: awscicd/jenkins-server
  files:
    - tfplan-${ENV}
    - "**/*"
  discard-paths: no
